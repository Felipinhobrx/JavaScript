function calcularIdade() {
    var inome = document.getElementById('nome')
    var nome = inome.value
    var iidade = document.getElementById('idade')
    var idade = iidade.value
    var imagem = document.getElementById('img')

    if(idade = 0 && idade < 8 && sexo == 'M') {
        imagem.img.src = 'imagens/bb H'
    }
}